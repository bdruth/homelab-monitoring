Deferred (as of 2026-06-14): refactor the Playwright dashboard test to authenticate through Authelia once and reuse cookies via `storageState`, instead of running the full login + TOTP + consent flow on every attempt.

**Why:** Only `tests/dashboard_app.spec.ts` uses Authelia/TOTP (workflow uses Mattermost's own form, indexer has no auth), so there is NO cross-worker race. The only residual risk is the dashboard test re-running TOTP on retries with an OTP captured once at job start (1Password `?attribute=otp`), which can go stale ~90s into a run. The flakiness fixes already applied (kill `networkidle` → `domcontentloaded`, conditional Authelia consent click, web-first "No Data" `toHaveCount` assertion) reduce spurious retries, so this is robustness, not correctness — safe to defer. User confirmed deferral.

**How to apply (when picked up):** add `tests/auth.setup.ts` that logs in once and saves `playwright/.auth/authelia.json`; add a `setup` project in `playwright.config.ts` with `chromium` `dependencies: ['setup']`; set `test.use({ storageState })` in the dashboard spec and delete its login block; gitignore `playwright/.auth/`. Relies on Authelia session cookie being scoped to the parent domain for forward-auth.